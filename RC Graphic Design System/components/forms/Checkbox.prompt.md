Checkbox with optional description line — used for add-ons (rounded corners, mail service) and consent.

```jsx
<Checkbox label="Add rounded corners" description="+$12 per 500" checked={on} onChange={…} />
```
