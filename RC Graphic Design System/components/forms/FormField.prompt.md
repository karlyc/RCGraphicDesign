Wraps any control with RC's condensed uppercase label and hint/error line.

```jsx
<FormField label="Quantity" hint="Price per piece drops at 500." htmlFor="qty">
  <Input id="qty" defaultValue="250" />
</FormField>
```
