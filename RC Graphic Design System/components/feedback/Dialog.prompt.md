Modal over a blurred charcoal scrim.

```jsx
<Dialog eyebrow="Step 2 of 4" title="Upload your artwork" onClose={close}
  footer={<><Button variant="ghost">Cancel</Button><Button>Continue</Button></>}>…</Dialog>
```
