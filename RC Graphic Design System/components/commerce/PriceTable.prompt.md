Quantity-break ladder. Rows are clickable when `onSelect` is given.

```jsx
<PriceTable selected={500} onSelect={setQty}
  rows={[{qty:250,each:'$0.14',total:'$35.00'},{qty:500,each:'$0.09',total:'$45.00',note:'Best value'}]} />
```
