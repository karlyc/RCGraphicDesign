Catalog tile. Always show a price floor and a turnaround — those are RC's two buying signals.

```jsx
<ProductCard name="Business cards" blurb="16pt, 3.5 x 2 in, full color both sides"
  startingAt="$24.50" turnaround="Next day" icon="credit-card" />
```

Pass `image` when a real photo exists; the placeholder is intentional and labelled otherwise.
