The RC brand mark. Use the `lockup` image wherever the real logo is available; use the typographic variants only when it is not.

```jsx
<Logo height={44} src="assets/logo-rc-lockup-dark.png" />
<Logo variant="onDark" height={40} />
```

The supplied artwork is a dark charcoal card, so it sits on dark or white surfaces without a knockout. Never recolor or redraw it.
