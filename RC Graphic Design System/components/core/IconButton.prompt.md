Icon-only control for header utilities (search, account, cart) and card toolbars.

```jsx
<IconButton icon="shopping-cart" label="Cart" badge={2} />
<IconButton icon="search" label="Search" variant="outline" />
```

Always pass `label`. `badge` renders the magenta count bubble.
